export const metadata = {
  title: 'Vehicle Tracker - Thadam',
  description: 'Track your vehicle location and ETA in real-time',
};

export default function PassengerLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
